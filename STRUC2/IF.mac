;
; IF.MAC - test code for STRUC2 structured programming macro calls. 
;
        SEARCH STRUC2
            ;LALL
        $STRUC

        Z 0,0(0)

        ;  ******************** Simple IF
        $IF <(TRUE)>
            Z 1,1(0)
        $ENDIF

        reset
        exit
        END
